# Compare

This crate is for benchmarking and comparing tiff2 to other tiff readers/writers

There is no limit on dev dependencies here: If you find a tiff reader that can be reasonably integrated, add it here.
